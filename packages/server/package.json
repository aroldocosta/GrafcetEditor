{
  "name": "@grafcet/server",
  "version": "1.0.0",
  "description": "Grafcet Editor Backend Server - HTTP Deploy & Compilation API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun x tsc"
  },
  "dependencies": {
    "@grafcet/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
